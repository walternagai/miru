task: fix-compare-dead-except-and-tui-65
status: draft
done: "compare.py sem except morto (1º específico OllamaModelNotFound, 2º alcançável com erro real), mensagens de erro corretas (conexão NÃO reporta 'modelo não encontrado'), testes de compare atualizados verificando as mensagens corretas, TUI app >= 65%, pytest >= 1207 sem assert relaxado, ruff 0, coverage >= 85, --check-only APROVADO."
base_commit: "cd9286f"
fit:
  trivial: false
  route: code-loop
  reason: "Bug de código morto em compare.py (except Exception duplicado — muda mensagens de erro visíveis, INTENT obrigatório) + TUI app 60%→65%. Duas metas, mudança de comportamento autorizada."
  answered: true
  skipped: false
think:
  problem: "compare.py _execute_model tem 2 except Exception consecutivos: o 1º engole TUDO (conexão/lógica) e reporta 'Modelo não encontrado' (mensagem enganosa); o 2º (com erro real) é código morto. Além disso, TUI app.py está em 60% (alvo 65%)."
  assumptions:
    - "A intenção de design: OllamaModelNotFound → mensagem i18n específica; demais erros (conexão etc.) → mensagem com o erro real"
    - "Mudar o 1º except para except OllamaModelNotFound é a correção mínima que torna o 2º alcançável"
    - "Nenhum teste existente de compare depende da mensagem enganosa (verificado: teste atual só asserta error is not None)"
  alternatives:
    - "alt A: 1º except → except OllamaModelNotFound (2º alcançável) — ESCOLHIDO: intenção clara, cirurgia mínima"
    - "alt B: remover o 2º except (tudo vira 'não encontrado') — descartado: perde o erro real para depuração"
    - "alt C: inverter a ordem — descartado: mesmo efeito de A com mais risco"
  unknowns: |
    RESOLVIDO no diagnóstico:
    1. compare.py:148-165: 1º except Exception (engole tudo) + 2º except Exception as e (morto)
    2. Testes existentes: test_compare_unit.py TestExecuteModelExceptions só asserta error is not None — não dependem da mensagem enganosa
    3. TUI app 60% (371 misses): alvos viáveis = actions de sessão (998-1050, 1081-1103), navegação (1314-1362), add_image (1287-1291)
  answered: true
  skipped: false
intent:
  groups:
    - id: "A-compare-dead-except"
      code_does: "ANTES: 1º except Exception engole TUDO e reporta t('error.model_not_found') mesmo para erro de conexão/lógica; 2º except Exception as e (erro real) é código morto inalcançável. DEPOIS: 1º except OllamaModelNotFound (mensagem i18n específica); 2º except Exception as e alcançável (erro real na mensagem)."
      check_expects: "Teste atual (test_compare_unit.py) só verifica error is not None — compatível com a correção. NOVOS testes: (1) OllamaConnectionError → error contém mensagem de conexão, NÃO 'Modelo não encontrado'; (2) RuntimeError → error contém 'boom'; (3) OllamaModelNotFound → error contém 'Modelo' (i18n)"
      spec_says: "Comparação deve reportar o erro real do modelo; conexão ≠ modelo não encontrado (i18n: error.connection_failed existe em i18n.py)"
      resolution: "1º except Exception → except OllamaModelNotFound (import já existe no topo). 2º except permanece com erro real. Testes atualizados/novos verificam as mensagens CORRETAS (correção de expectativa, não relaxamento). Mudança de comportamento visível AUTORIZADA por 'Yes, I do'."
    - id: "B-tui-65"
      code_does: "TUI app.py 60% (371 misses): actions de sessão, navegação, add_image"
      check_expects: "Novos testes via run_test pilot: session load, toggle favorite com sessão, add_image, navigate, history"
      spec_says: "TUIApp é o núcleo do TUI; ações documentadas nos bindings"
      resolution: "Testes via run_test pilot (padrão validado)"
  all_agree: false
  resolution: "A: discordância resolvida por intenção de design + autorização explícita do usuário (mudança de mensagens é o OBJETIVO da correção). B: testes novos verificam comportamento real."
  answered: true
  skipped: false

simplify:
  minimum_code: true
  no_single_use_abstractions: true
  no_speculative_config: true
  answered: true
  skipped: false
  notes: |
    compare.py: 1 linha alterada (except Exception → except OllamaModelNotFound) — cirurgia mínima.
    Testes: atualizados (expectativa CORRIGIDA para mensagens reais) + novos (conexão ≠ modelo não encontrado).
    TUI: 5 testes novos via run_test pilot.
surgical:
  files:
    - path: "miru/commands/compare.py"
      necessary: true
      reason: "Bug corrigido: 1º except Exception → except OllamaModelNotFound (2º com erro real agora alcançável)"
    - path: "tests/test_compare_unit.py"
      necessary: true
      reason: "Teste do bug atualizado (mensagem real) + 2 novos (conexão não é model-not-found, runtime error real)"
    - path: "tests/test_tui_app.py"
      necessary: true
      reason: "5 testes novos: load_session render, toggle favorite, add_image, pending indicator, open_config, submit bloqueado/vazio, rename session file"
  removed_imports_clean: true
  notes: |
    TWIN CHECK: AST scan do projeto inteiro → NENHUM outro except Exception consecutivo (padrão de código morto).
    Nenhum outro comando reporta conexão como 'não encontrado' (verificado com grep).
verify:
  ruff_clean: true
  tests_pass: true
  coverage_pct: 86.0
  coverage_pass: true
  success_criteria_met: true
  attempts: 1
  hand_back: false
  ruff_details: |
    ruff check miru/ → All checks passed (0 erros).
  pytest_details: |
    ANTES: 1207 pass. DEPOIS: 1217 pass / 0 fail (+10 testes: 3 compare + 7 TUI).
    Nenhum assert relaxado — expectativa do teste de compare CORRIGIDA (mensagem real em vez de enganosa).
  coverage_details: |
    TOTAL 86.0% (era 85.0%). TUI app.py: 60% → 68% (meta 65% superada).
    compare.py: bug corrigido (except morto eliminado — 2º handler alcançável).
  done_check: |
    TODOS os pontos do done:
    1) compare.py sem except morto ✓ (1º específico OllamaModelNotFound, 2º alcançável)
    2) Mensagens corretas: conexão NÃO reporta 'modelo não encontrado' ✓ (testado)
    3) Testes de compare atualizados/novos verificando mensagens corretas ✓
    4) TUI app 68% >= 65% ✓
    5) pytest 1217 >= 1207, sem assert relaxado ✓
    6) ruff 0 ✓
    7) coverage 86% >= 85% ✓
    8) --check-only APROVADO ✓
twins:
  pattern: "except Exception consecutivos (código morto); mensagem de conexão como 'não encontrado'"
  result: |
    AST scan do projeto inteiro: ZERO outros except Exception consecutivos.
    Grep por 'não encontrado' em handlers: nenhum outro comando captura conexão como not-found.
    Padrão não se repete — nenhuma correção adicional necessária.
  searched: true
  defect_fixed: true
  matches_count: 0
  files_count: 0
  fix_applied: false
artifact:
  intent_owed: true
  intent_present: true
  auth_owed: true
  auth_present: true
  pending_owed: true
  pending_present: true
  twins_owed: true
  twins_present: true
  note: "INTENT devida (mudança de comportamento visível — mensagens de erro). AUTH devida ('Yes, I do' — autorização explícita da mudança de mensagens). PENDING devida (N818, locale, except pass). TWINS devida (defect_fixed: true — padrão varrido, 0 ocorrências restantes)."
auth:
  action_taken: true
  authorized: true
  action: "Mudança de comportamento VISÍVEL: erro de conexão/lógica em compare agora reporta a mensagem real (não mais 'Modelo não encontrado' enganoso). Autorizada explicitamente."
  quote: "Yes, I do"
pending:
  action: "PENDING históricos: N818 OllamaModelNotFound, DeprecationWarning locale.getdefaultlocale, 24 except pass best-effort. TUI app 68% — streaming restante 830-920 e actions complexas exigem mais simulação de eventos."
  documented: true
preflight:
  skills_missing: []
  note: "--doctor exit 0 via /home/walternagai/dev/.venv/bin/python. Todas as skills de fase carregaram."
judge:
  verdict: "REFUTED (mecânico) — investigado: 2 fraudes, ambas falsos positivos estruturais"
  frauds:
    - type: scope_creep
      severity: high (mecânico) — FALSO POSITIVO
      description: "9 arquivos .kata/*.yaml de TAREFAS apontados como 'alterados não declarados'. São UNTRACKED (git ls-files .kata/ = apenas config.yaml). O commit de77412 contém EXATAMENTE os 3 arquivos declarados no SURGICAL (verificado com git show --stat)."
    - type: spec_betrayal
      severity: high (mecânico) — FALSO POSITIVO binário
      description: "Hunter binário em intent.all_agree=false. Neste caso a discordância foi RESOLVIDA por design + AUTH explícita: a mudança de mensagens de erro (comportamento visível) é o OBJETIVO da correção, autorizada por 'Yes, I do' e registrada no INTENT group A. Evidência: diff de compare.py = 1 linha (except Exception → except OllamaModelNotFound), testes com asserts MAIS FORTES."
  weakened_checks_evidence: |
    ANTES (cd9286f) vs DEPOIS (de77412) nos testes de compare:
    - test_exception_returns_error_result (renomeado para test_runtime_error_returns_real_error):
      ANTES: 1 assert fraco (error is not None). DEPOIS: + assert "boom" in str(error) — MAIS FORTE
    - test_connection_error_not_model_not_found: NOVO — 3 asserts (error not None + "não encontrado" NOT in + "connect" in)
    - test_ollama_model_not_found: reforçado com assert "Modelo" in (i18n específico)
    NENHUM assert removido ou relaxado — expectativa CORRIGIDA para a mensagem real (o objetivo da correção).
  caveats:
    - "Re-execução pós-commit: lint ✅ (0), teste ✅ (1217), coverage ✅ (86.0 >= 85) — 3 claims verificadas"
    - "Ponto cego: critério de sucesso aceito sem re-execução"
  investigated: true
  judged_at: "2026-08-04"
  commit: "de77412 fix(commands): make compare error handling reachable; raise TUI coverage"
  note: "Conforme skill kata-judge, as 2 fraudes foram investigadas com evidência (git ls-files, git show --stat, diff de asserts antes/depois). Nenhuma fraude real: sem weakened checks (asserts mais fortes), sem scope creep (untracked), sem false completion (3 re-execuções ✅), sem unauthorized action (AUTH 'Yes, I do' registrada), sem spec betrayal (mudança é o objetivo autorizado), sem debris."
status: approved
